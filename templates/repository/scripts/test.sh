#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

# TODO: sustituir por las pruebas reales del repositorio (pytest, npm test, tofu validate, etc.)
echo "test OK (sin pruebas definidas todavía)"
