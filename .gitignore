# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Python (validators/generators tooling)
__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/

# Node (if tooling added later)
node_modules/

# Secrets and local overrides — never commit
.env
.env.*
*.local.yaml
/secrets/

# Generated evidence/reports (must live in Ceph RGW / OCI artifacts, not Git).
# Anclado a la raíz (/evidence/): un patrón sin la barra inicial coincide
# con CUALQUIER directorio "evidence" del árbol, y silenciosamente dejó
# fuera de git a templates/evidence/evidence-manifest-template.json desde
# siempre — un archivo real, referenciado por
# templates/runbook/runbook-template.md, que nunca llegó a versionarse.
# Mismo bug encontrado y corregido en argos-validation
# (harness/evidence/manifest.py, revisando `git status --ignored`).
/evidence/
reports/generated/
