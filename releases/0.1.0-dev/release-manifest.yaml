release: 0.1.0-dev
status: dev
created_at: "2026-08-17T09:55:00Z"

# Actualizado para ARG-028 (as-built, propuesta v0.6.25.4 §16.7) — la
# versión anterior solo registraba el "Initial commit" de ARG-001 (S1),
# desincronizada del estado real tras S2-S8. `images`/`contracts`/
# `validation` siguen omitidos (permitido por el schema para status=dev):
# ningún componente OSS tiene versión/digest fijado todavía
# (helm/argos-services/Chart.yaml sigue con `version: "TODO"`,
# `opensearch.enabled: false` — ver
# argos-validation/traceability.yaml gate G0/G6 para el detalle real de
# qué falta antes de poder pasar a status=candidate).

repositories:
  argos-platform:
    commit: a701a0b
  argos-contracts-scenarios:
    commit: 01e8f92
  argos-core:
    commit: 9777efa
  argos-cyber-tools:
    commit: ef4823c
  argos-validation:
    commit: a244da5
  argos-smartops:
    commit: e581d3b
